/*:
 # Swift 5.5
 * [Swift Concurrency](https://github.com/LucasAbijmil/Swift-Playgrounds/tree/master/Swift%20Concurrency)
 * [Computed Property](Computed%20Property) : Les computed properties `get` only supportent `async` & `throws` utilisé ensemble ou séparément
 * [Double - CGFloat cast implicit](Double%20-%20CGFloat) : Cast implicite `CGFloat` vers `Double` et vice-versa
 * [Lazy var dans un context local](Lazy%20var%20in%20Local%20Context) : `lazy var` désormais possible dans le scope de fonctions
 * [#if check avec des expressions postfix](if%20postfix%20member%20expressions) : Il est désormais possible d'effectuer des `#if` avec des expressions postfixs
 * [Protocols dans une fonction générique](Extending%20static%20member%20lookup) : Permet d'utiliser des protocols dans une fonction générique comme des cases d'une `enum`
 * [Enum sans RawValue conforme à Codable](Enum%20Codable%20without%20RawValue) : Les `enum` non conformes à `RawRepresentable` peuvent être conforme à `Codable` sans extra work
 */
//: La majorité des exemples sont tirés du site [What's new in Swift](https://www.whatsnewinswift.com/?from=5.4&to=5.5).
