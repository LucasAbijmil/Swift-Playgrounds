/*:

 # Enum Extensions & tricks
 * [enum sans associated value](Enum%20sans%20associated%20value) : création d'une `enum` sans `associated value`
 * [enum avec associated value](Enum%20avec%20associated%20value) : création d'une `enum` avec `associated values`
 * [switch pattern matching](Switch%20pattern%20matching) : pattern matching dans un `switch`
 * [if case pattern matching](If%20case%20pattern%20matching) : `if case` pattern matching
 * [guard case pattern matching](Guard%20case%20pattern%20matching) : `guard case` pattern matching
 * [for case pattern matching](For%20case%20pattern%20matching) : `for case` pattern matching
 * [enum conforme au protocol Codable](Enum%20Codable) : rendre une `enum` conforme au protocol `Codable`
 * [indirect enum](Indirect%20Enum) : rendre une `enum` récursive avec `indirect`
 */
