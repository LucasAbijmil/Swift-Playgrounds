/*:
 # Swift Concurrency
 * [Async await](Async%20Await) : fonction asynchrone marquée `async` & appelle à celle-ci
 * [Async throws functions](Async%20Await%20Throws) : fonction asynchrone qui peut générer une erreur, marquée `async throws` et appelle à celle-ci
 * [Async URLSession](Async%20URLSession) : exemple d'un call API avec `async` `await` et `URLSession`
 * [Async Let Binding](Async%20Let%20Binding) : exécuter des fonctions `async` en parallèle grâce aux `async let` bindings
 * [AsyncSequence](AsyncSequence) : `AsyncSequence` est un protocol similaire à `Sequence` permettant un accès en asynchrone
 * [Completion to Async](Completion%20to%20Async) : bridger des fonctions asynchrones avec completion vers une fonction asynchrone marquée `async` avec un return
 * [Structured Concurrency](Structured%20Concurrency) : exécution et monitoring d'opérations concurrentes dans un contexte asynchrone
 * [Actors](Actors) : `actor` le remplaçant des classes dans un contexte asynchrone
 * [MainActors](MainActors) : un nouveau wrapper permettant d'exécuter du code uniquement sur le main thread
 * [Sendable & @Sendable](Sendable%20&%20@Sendable) : transmettre des données à un autre thread de manière sécurisée
 */
//: Note : La plus part des exemples sont tirés d'ici : https://www.whatsnewinswift.com/?from=5.4&to=5.5
