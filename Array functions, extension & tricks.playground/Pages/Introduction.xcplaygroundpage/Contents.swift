/*:
 # Fonctions sur les Arrays :
 
 * [`enumerated()`](Enumerated) : accès à l'index et la value d'un array sans avoir à gérer l'index et subscript à l'array
 * [`zip(array1, array2)`](Zip) : boucler simultanément sur deux collections d'indice différents
 * [`Safe Subscript`](SafeSubscript) : permet de renvoyer `nil` au lieu d'une `fatalError` si on subscript à un indice non contenu dans une `Collection`

 */
